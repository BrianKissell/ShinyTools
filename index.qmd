---
title: "ShinyTools"
filters: 
   - shinylive
---

Will this work, if I write something here?

```{shinylive-r}
#| standalone: true

library(shiny)

ui <- fluidPage(
  selectInput("inputid", "What the what?", c("1", "2", "3"))
)

server <- function(input, output, session) {
  
}

shinyApp(ui, server)


```
